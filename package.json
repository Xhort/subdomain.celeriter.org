{
  "name": "drip-dye-storefront",
  "version": "1.0.0",
  "description": "Simple Drip Dye storefront with Stripe checkout and order database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check script.js && node --check server.js && node --check backend/database.js && node --test",
    "stripe:limits": "node scripts/configure-stripe-limits.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "pg": "^8.13.1",
    "stripe": "^16.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
